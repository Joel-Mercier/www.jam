import UserList from "@/components/users/UserList";
import { Box, Input, InputField, InputIcon, InputSlot, SearchIcon, Text } from "@gluestack-ui/themed";

export default function LoginScreen() {
  return (
    <Box px="$4">

    </Box>
  )
}